## 2025-02-14 - Native lazy loading optimization
**Learning:** Adding `loading="lazy"` to a modal with many images is effective, but always include performance-oriented explanatory comments as required by agent constraints, even if the code itself is simple and standard.
**Action:** In future optimizations, always pair the code change with a comment detailing the 'what' and 'why' for performance impact, especially when specifically prompted to do so.
